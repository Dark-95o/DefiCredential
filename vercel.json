{
  "buildCommand": "npm run build --workspace=ui",
  "outputDirectory": "ui/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
